# 项目语义注册表（通用模板）

> **文件路径**：`Docs/governance/PROJECT_SEMANTIC_REGISTRY.md`  
> **定位**：本文件定义当前项目的【危险实体】、【高优先级热路径】及【影响半径映射】。  
> **使用方式**：复制本文件到项目治理目录，将 `[填写...]` 替换为项目实际的类名、文件名和关联关系。协作协议内核在 RISK_SCAN 状态自动加载本文件。

---

## 一、Critical Systems Registry（核心危险实体）

修改触及以下实体时，自动触发最高级别安全检查与自升级。

| 锚点 ID | 实体（类名/文件名） | 风险等级 | 强制动作 |
|---------|-------------------|----------|----------|
| `ENT-CRIT-001` | [填写核心实体1] | P0/P1 | [填写强制检查项] |
| `ENT-CRIT-002` | [填写核心实体2] | P0/P1 | [填写强制检查项] |
| `ENT-CRIT-003` | [填写核心实体3] | P0/P1 | [填写强制检查项] |
| `ENT-CRIT-004` | [填写核心实体4] | P0/P1 | [填写强制检查项] |
| `ENT-CRIT-005` | [填写核心实体5] | P0/P1 | [填写强制检查项] |

> 按需增减行数。风险等级：P0 为运行时崩溃级，P1 为数据/持久化级。

---

## 二、High Risk Paths（T1 硬检查热路径）

即使处于 T1 快速执行模式，触及以下类名或文件名时仍须强制执行对应检查。

| 锚点 ID | 触发词（类名/文件名包含） | 强制执行的检查 |
|---------|--------------------------|----------------|
| `ENT-T1-001` | [填写触发词1] | [填写检查内容] |
| `ENT-T1-002` | [填写触发词2] | [填写检查内容] |
| `ENT-T1-003` | [填写触发词3] | [填写检查内容] |
| `ENT-T1-004` | [填写触发词4] | [填写检查内容] |

---

## 三、Impact Radius Mapping（影响半径映射）

定义修改某实体时，必须联动检查的关联系统。

| 被修改实体 | 须检查的关联系统 |
|-----------|----------------|
| [填写实体A] | [填写关联系统列表] |
| [填写实体B] | [填写关联系统列表] |
| [填写实体C] | [填写关联系统列表] |
| [填写实体D] | [填写关联系统列表] |

> 关联系统以列表形式填写，如：`SystemA, SystemB, SystemC`。