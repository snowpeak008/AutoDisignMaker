# Naming Convention

- Stage directories use `stage_XX`.
- Artifact IDs use `stage_XX.name_bundle`.
- Task IDs use `stage_XX.action_name`.
- Artifact layer reports use:
  - `artifact_layer_manifest.json`
  - `artifact_reviews.json`
  - `artifact_validation_layer.json`

