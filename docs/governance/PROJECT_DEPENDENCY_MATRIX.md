# 项目依赖矩阵（通用模板）

> **文件路径**：`Docs/governance/PROJECT_DEPENDENCY_MATRIX.md`  
> **定位**：本文件定义项目模块间的【禁止依赖】关系。  
> **使用方式**：以命名空间或模块名为单位填写禁止直接引用对，并指定唯一允许的通信方式。

## 禁止依赖表

| 锚点 ID | 源模块（禁止引用方） | 目标模块（被引用方） | 允许的通信方式 |
|---------|--------------------|-------------------|----------------|
| `DEP-001` | [填写源模块] | [填写目标模块] | [填写通信方式] |
| `DEP-002` | [填写源模块] | [填写目标模块] | [填写通信方式] |
| `DEP-003` | [填写源模块] | [填写目标模块] | [填写通信方式] |
| `DEP-004` | [填写源模块] | [填写目标模块] | [填写通信方式] |

> 通信方式通常为：仅通过事件系统、通过接口隔离、禁止直接引用等。

## 全局边界规则（无需修改）

- 业务层禁止直接引用 UI 层，UI 只能通过事件监听更新。
- 具体违反判断由协作协议内核处理。