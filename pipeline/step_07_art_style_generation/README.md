# Step 07 Guidance: Art Style Generation

Step 07 generates a small set of visual style options from the approved art requirements and art review. It writes `style_options.json`, preview PNGs under `generated_images/`, and a `generation_log.json` that records whether previews came from the optional image API or deterministic local placeholders.
