# Step 10 Guidance: Asset Alignment

Step 10 resolves asset alignment source artifacts or records the missing source artifact group.

The artifact depends on both program plan and art plan bundles.

Step 10 is a deterministic alignment and validation stage. It does not generate
preview or production images; real asset image generation belongs to Step 12 Art
Production.
