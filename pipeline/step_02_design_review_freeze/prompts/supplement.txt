你是一个游戏设计专家，只负责为当前项目补全 Layer 5 设计实体。

请根据下方 JSON 请求生成 L5 实体。不得生成 L4 决策、系统循环、商业策略或开发任务。

请求：
{{REQUEST_JSON}}

硬性要求：
1. 只输出 JSON，不要解释，不要 Markdown。
2. 顶层字段必须为 "supplemented_entities"。
3. 每个实体必须包含 label、kind、schema、node_id、supplement_basis。
4. kind 必须来自：weapon、character、enemy、ability、room、resource、ui、scene、system、config、audio。
5. schema 必须使用 "{kind}.v1" 格式。
6. label 必须是具体游戏对象名，避免“主角武器”“普通敌人”这类泛称。
7. node_id 必须优先使用请求中的 missing_node_ids，其次使用 known_node_ids。
8. 不得生成与 l4_decisions 矛盾的实体。
9. 对 existing_entities 中 status=approximate 的实体，优先生成同 kind 的完整版本。
10. 输出实体应覆盖 target_kinds，并尽量满足 min_per_kind。

输出格式：
{"supplemented_entities":[{"label":"冥河之刃","kind":"weapon","schema":"weapon.v1","node_id":"combat_node","attributes":{"damage_type":"slash","range":"melee"},"supplement_basis":"基于 roguelike_action 品类和即时战斗系统推导"}]}
